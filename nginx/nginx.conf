server {
    listen 80;
    server_name video-dubber-api.similartoolz.app;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

# server {
#     listen 443 ssl;
#     http2 on;

#     server_name video-dubber-api.similartoolz.com;

#     ssl_certificate /etc/letsencrypt/live/video-dubber-api.similartoolz.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/video-dubber-api.similartoolz.com/privkey.pem;

#     ssl_protocols TLSv1.2 TLSv1.3;

#     client_max_body_size 500M;

#     location / {
#         proxy_pass http://fastapi:8000;

#         proxy_http_version 1.1;

#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto https;

#         proxy_read_timeout 3600;
#         proxy_connect_timeout 3600;
#         proxy_send_timeout 3600;
#     }
# }